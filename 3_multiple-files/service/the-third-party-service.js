// Import axios (e.g)

// Prepare axios headers etc..


function forwardId(/* id */) {
  // fetch('/do-something', {
  //   method: 'POST'
  //   body: { id },
  //   mode: etc...
  // }).catch(...);
}

module.exports = {
  forwardId,
}